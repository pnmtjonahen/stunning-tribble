---
# comment in manifest
applications:
    - name : ptj-movie-review
      buildpack: java_buildpack
      path: target/review-0.0.1-SNAPSHOT.jar
      services:
        - myMysqlDB
        - myServiceDiscovery
